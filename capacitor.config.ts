import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'utopia.hahucomputers.com',
  appName: 'Utopia Home Assistant',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
